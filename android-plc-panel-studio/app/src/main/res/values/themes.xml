<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.RorkApp" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Same navy as the loading page, so the window never flashes white while the app starts. -->
        <item name="android:windowBackground">#FF00263F</item>
    </style>
</resources>
